#torch>=2.0.0
torch-geometric>=2.5.0
scipy>=1.10
matplotlib>=3.7

# Node2Vec + Logistic Regression — Link Prediction
# Python >= 3.9 recommended

# PecanPy: fast Node2Vec via numba JIT + on-the-fly transition probabilities.
# Replaces the slow `node2vec` library (which precomputes all probs upfront).
# Install numba separately first if on Apple Silicon: conda install numba
pecanpy>=2.0.4            # Node2Vec walks — SparseOTF / DenseOTF / PreComp modes
numba>=0.57.0             # JIT backend for pecanpy walk generation
gensim>=4.3.0             # Word2Vec, KeyedVectors
scikit-learn>=1.3.0       # LogisticRegression, StandardScaler, Pipeline, metrics
numpy>=1.24.0
pandas>=2.0.0
networkx>=3.1
joblib>=1.3.0             # model serialisation
tqdm>=4.65.0              # progress bars
